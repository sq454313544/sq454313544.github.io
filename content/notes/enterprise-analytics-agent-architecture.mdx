---
title: "企业智能问数架构复盘：LangGraph、MCP 与确定性执行"
description: "复盘企业智能问数系统如何划分理解、检索、受控能力与执行职责，并通过 LangGraph、MCP 和评测机制降低不确定性。"
publishedAt: "2026-07-28"
updatedAt: "2026-07-28"
category: "agent"
tags: ["enterprise-data-agent", "langgraph", "mcp", "architecture", "agent"]
draft: false
featured: false
---

## 专题导航

**企业智能问数工程专题**

- [Registry First：让智能问数从 Prompt 驱动走向契约驱动](/notes/registry-first-for-enterprise-analytics)
- [受控查询：Query MCP 如何建立 SQL 安全边界](/notes/controlled-query-mcp-security)
- 本篇：企业智能问数架构复盘：LangGraph、MCP 与确定性执行

## 先定义边界，而不是先堆组件

企业智能问数通常同时面对表达不确定、知识变化、权限控制和数据执行等问题。架构的关键不是使用多少模型或工具，而是让每一层只承担适合它的职责：模型理解问题，知识层提供可用能力，受控服务执行确定性动作，应用层对结果负责。

## 分层架构

<MermaidDiagram chart={`flowchart TB
    A[用户与产品入口] --> B[LangGraph 工作流]
    B --> C[意图与上下文]
    C --> D[Metadata MCP]
    D --> E[能力注册表]
    B --> F[Query MCP]
    F --> G[受控执行层]
    G --> H[结果解释与导出]
    I[评测、日志与反馈] --> B
    I --> E`} />

### LangGraph：管理状态与分支

工作流需要处理澄清、路由、工具调用、失败恢复和追问。图结构适合显式表达状态与分支：每个节点只处理一类职责，节点之间传递必要状态，而不是把所有逻辑塞进单轮提示词。

### Metadata MCP：把知识提供给模型

Metadata MCP 不直接执行查询，它负责把注册表中的可用能力转化为模型可理解的说明。这样业务定义能够独立演进，模型上下文也可以按问题按需提供，而非写死在 Prompt 中。

### Query MCP：把执行收敛到唯一入口

Query MCP 接收经过能力匹配的请求，完成权限、参数、语法和结果规模校验，再调用确定性执行能力。它与 Metadata MCP 分离，避免“知道数据”天然等于“可以执行数据操作”。

## 检索与确定性执行如何配合

检索适合解决“有哪些已知能力、哪些说明与当前问题相关”；确定性执行适合解决“在已确认边界内如何得到结果”。把两者混为一谈，容易让检索内容被误当成执行指令，或让模型在信息不足时自行补全规则。

## 失败降级与可观测性

系统应把失败看作正常分支：能力不匹配时请求澄清，权限不足时给出可理解的边界说明，执行异常时保留可追溯状态并避免重复提交。评测样本、运行日志和人工反馈共同驱动后续改进，但公开文章只描述方法，不公开用户请求或业务数据。

## 常见追问

### 为什么需要 RAG？

RAG 适合检索说明、示例和能力元数据，但不能替代口径契约或权限校验。它提供相关上下文，最终选择和执行仍要经过受控流程。

### 如何避免工作流越来越复杂？

先定义稳定的输入、输出和失败状态，再让节点按职责拆分。高频流程保持短链路，低频或高风险分支通过明确的澄清与人工处理入口降级。

### 如何评估系统是否变好？

除回答文本外，还要观察能力路由是否正确、受控执行是否符合预期、失败是否被安全处理，以及变更后关键样本是否回归。评测集应随真实问题持续补充和去敏。

## 更新记录

- 2026-07-28：首版发布，沉淀系统分层、组件职责和失败降级方法。

> 本文为工程方法复盘，不包含真实架构配置、内部能力清单、业务数据或运行日志。
