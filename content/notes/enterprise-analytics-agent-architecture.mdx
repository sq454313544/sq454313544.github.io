---
title: "企业智能问数架构复盘：LangGraph、MCP 与确定性执行"
description: "复盘 LangGraph、Registry 与 MCP 如何协同，并说明图工作流相对线性 Chain 在状态、分支、重试和追问上的价值。"
publishedAt: "2026-07-28"
updatedAt: "2026-07-30"
category: "agent"
tags: ["enterprise-data-agent", "langgraph", "mcp", "architecture", "agent"]
draft: false
featured: false
---

## 专题导航

**企业智能问数工程专题**

- [Registry First：让智能问数从 Prompt 驱动走向契约驱动](/notes/registry-first-for-enterprise-analytics)
- [受控查询：Query MCP 如何建立 SQL 安全边界](/notes/controlled-query-mcp-security)
- 本篇：企业智能问数架构复盘：LangGraph、MCP 与确定性执行
- [Agent 工具调用质量：从“选对工具”到端到端成功](/notes/agent-tool-calling-evaluation)
- [Agent Planner 优化：确定性、缓存与成本控制](/notes/agent-planner-optimization)

## 先定义边界，而不是先堆组件

企业智能问数通常同时面对表达不确定、知识变化、权限控制和数据执行等问题。架构的关键不是使用多少模型或工具，而是让每一层只承担适合它的职责：模型理解问题，知识层提供可用能力，受控服务执行确定性动作，应用层对结果负责。

## 分层架构

<MermaidDiagram chart={`flowchart TB
    A[用户与产品入口] --> B[LangGraph 工作流]
    B --> C[意图与上下文]
    C --> D[Metadata MCP]
    D --> E[能力注册表]
    B --> F[Query MCP]
    F --> G[受控执行层]
    G --> H[结果解释与导出]
    I[评测、日志与反馈] --> B
    I --> E`} />

### LangGraph：管理状态与分支

工作流需要处理澄清、路由、工具调用、失败恢复和追问。图结构适合显式表达状态与分支：每个节点只处理一类职责，节点之间传递必要状态，而不是把所有逻辑塞进单轮提示词。

### Metadata MCP：把知识提供给模型

Metadata MCP 不直接执行查询，它负责把注册表中的可用能力转化为模型可理解的说明。这样业务定义能够独立演进，模型上下文也可以按问题按需提供，而非写死在 Prompt 中。

### Query MCP：把执行收敛到唯一入口

Query MCP 接收经过能力匹配的请求，完成权限、参数、语法和结果规模校验，再调用确定性执行能力。它与 Metadata MCP 分离，避免“知道数据”天然等于“可以执行数据操作”。

## 一次请求中三者如何协同

三者不是平级堆叠的组件，而是一条职责清晰的信息加工链：

1. LangGraph 接收问题与可信用户上下文，判断需要回答、澄清还是拒绝。
2. Metadata MCP 从 Registry 检索当前可用的能力候选。
3. LangGraph 把候选与会话状态合并，选择能力或发起澄清。
4. Query MCP 校验结构化参数并执行受控查询。
5. LangGraph 根据执行状态生成解释、建议或可恢复的失败反馈。

Registry 定义“系统知道什么”，MCP 提供“系统能做什么”，LangGraph 决定“此刻应走哪一步”。任何一层都不应越权代替另外两层。

## 为什么不是普通 Prompt Chain

线性 Chain 适合步骤固定、失败后可以整体重跑的任务；问数流程往往包含分支、追问和局部恢复。选择图工作流，主要是为了显式管理四类复杂性：

| 复杂性 | 图工作流的处理方式 |
|---|---|
| 结构化状态 | 保存能力候选、权限摘要、参数、执行状态与重试原因 |
| 条件路由 | 在回答、澄清、拒绝和执行之间选择分支 |
| 局部重试 | 只重跑失败节点，不重复已经确认的意图与权限判断 |
| 多轮追问 | 从检查点恢复已确认上下文，只增量修改当前条件 |

代价也很明确：状态类型、节点输入输出和循环条件必须被严格定义。流程较短且没有分支时，普通函数或简单 Chain 反而更容易维护。

## 检索与确定性执行如何配合

检索适合解决“有哪些已知能力、哪些说明与当前问题相关”；确定性执行适合解决“在已确认边界内如何得到结果”。把两者混为一谈，容易让检索内容被误当成执行指令，或让模型在信息不足时自行补全规则。

## 失败降级与可观测性

系统应把失败看作正常分支：能力不匹配时请求澄清，权限不足时给出可理解的边界说明，执行异常时保留可追溯状态并避免重复提交。评测样本、运行日志和人工反馈共同驱动后续改进，但公开文章只描述方法，不公开用户请求或业务数据。

重试需要有明确范围和预算：检索无结果可以换检索策略，参数校验失败应回到参数修正，执行超时可以建议缩小范围；权限拒绝则不应重试。循环必须设置终止条件，防止工作流把同一个错误不断送回模型。

## 常见追问

### 为什么需要 RAG？

RAG 适合检索说明、示例和能力元数据，但不能替代口径契约或权限校验。它提供相关上下文，最终选择和执行仍要经过受控流程。

### 如何避免工作流越来越复杂？

先定义稳定的输入、输出和失败状态，再让节点按职责拆分。高频流程保持短链路，低频或高风险分支通过明确的澄清与人工处理入口降级。

### 如何评估系统是否变好？

除回答文本外，还要观察能力路由是否正确、受控执行是否符合预期、失败是否被安全处理，以及变更后关键样本是否回归。评测集应随真实问题持续补充和去敏。

### 哪些场景需要人工介入？

高风险数据申请、业务口径冲突和连续多次无法消歧的请求，适合转入人工确认。Human in the Loop 应是明确的状态与权限边界，而不是任何失败后的默认兜底。

### MCP 调用失败后能否换一个相近能力？

只有在用户明确同意且结果中清楚标注口径变化时才可以。静默替换会把基础设施故障变成业务答案错误，通常比直接说明暂时无法完成更危险。

## 更新记录

- 2026-07-30：补充 LangGraph、Registry、MCP 协同链路，线性 Chain 对比与局部恢复边界。
- 2026-07-28：首版发布，沉淀系统分层、组件职责和失败降级方法。

> 本文为工程方法复盘，不包含真实架构配置、内部能力清单、业务数据或运行日志。
